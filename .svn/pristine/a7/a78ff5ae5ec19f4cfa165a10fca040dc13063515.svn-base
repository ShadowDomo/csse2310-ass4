#include "mapper.h"


/** Entry point to program. **/
int main(int argc, char** argv) {
    WorldState* worldState = malloc(sizeof(WorldState));
    worldState->countAirports = 0;
    setup_sockets(worldState, 0);
    return 0;
}
