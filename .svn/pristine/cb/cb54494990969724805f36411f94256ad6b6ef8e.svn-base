#include "mapper.h"








/** Entry point to program. **/
int main(int argc, char** argv) {
//    printf("hi");
//    semaphore_tester();
    WorldState* worldState = malloc(sizeof(worldState));
    worldState->countAirports = 0;
//    worldState->airports = malloc(sizeof(Airport*));
    make_socket(worldState, 0, -1);
    return 0;
}
